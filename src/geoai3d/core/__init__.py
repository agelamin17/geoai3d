"""Core data structures shared across GEOAI_3D.

This subpackage holds the foundational, dependency-light building blocks the
rest of the package builds on: the columnar ``PointCloud`` representation and
the ``Provenance`` lineage record it carries. Nothing here needs a GPU or a
compiler.
"""
